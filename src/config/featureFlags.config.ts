export const FEATURE_FLAGS = {
  /** Bulk import via sheet — Phase 1 (active: barcode/ชื่อ/SKU/แบรนด์/หมวด/สต็อก/ราคา + partial success) */
  sheetImport: true,
  /** Supplier management — in development */
  supplier: false,
} as const;
